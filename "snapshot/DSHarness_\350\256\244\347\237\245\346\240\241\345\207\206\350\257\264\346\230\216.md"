# DSHarness · 认知校准层说明（公开版）

## 定位（Positioning）

DSHarness 是一套**认知校准层（cognitive-calibration layer）**，运行在通用 Agent Harness（如 WorkBuddy / Qoder 等编排框架）之上，专门补足 Harness 最易被忽略的一块：**对齐与谄媚安全**。

它不重复造 Harness（上下文、工具调用、终端执行、权限控制由基座负责），而是解决「基座把模型变成生产力之后，谁来保证模型不滑向迎合」的问题。

## 核心机制（anti-FEE 操作化）

- **校准范式**：不给正确答案，给位置信号——让 AI 基于历史上下文判断「是否符合方向」而非「是否符合指令」。
- **边界检测**：识别模型在「择」中的策略是否偏离用户已表达的「正」（方向）。
- **独立判断（诤臣式）**：协议要求 AI 在检测到人类偏差时提出异议，而非顺承。

## 三阶段路线（Roadmap）

- **阶段一（当前）**：个人验证（PDSS PoC）——作者本人在真实长周期协作中验证。
- **阶段二**：泛化到 1–3 人小协作单元。
- **阶段三**：公开破冰稿，形成可多人遵循的协议。

当前处于阶段一，尚未到产业对接期。护城河不在能力壁垒，而在「验证过程即资产」与「方向源须装制动」的不可复制性。

## 与 Harness 招募的关系（Relevance）

DeepSeek Harness 团队在补「把聪明变成执行」的系统；DSHarness 补的是「执行过程中不滑向迎合」的校准层。两者同指 Agent 下半场——谁离用户的工作流与判断质量更近，谁拿到入口。
